FABRIC_MODE=mock
# live requires Keycloak (later) plus the server-only vars below. Never use NEXT_PUBLIC_.
# JSON-2 GREEN lots attach when all four Odoo vars are set; FABRIC_MODE stays mock until Keycloak.

# ODOO_URL=https://sattva.trilokventures.org
# ODOO_DB=sattva
# ODOO_USERNAME=middleware.bff
# ODOO_API_KEY=
# User must be in Odoo group "Middleware BFF" (not n8n Fabric Service).
# ODOO_BUYER_PARTNER_ID=

# N8N_BASE_URL=https://n8n.trilokventures.org
